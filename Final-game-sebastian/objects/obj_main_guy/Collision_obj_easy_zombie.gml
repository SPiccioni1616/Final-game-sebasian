// Reduce health when colliding with a ZOMBIE

health -= 5;  // Decrease health by 10 points (adjust as necessary)

//if (knockback_timer <= 0) {
//    var knockback_force = 6;
///    var dir = point_direction(other.x, other.y, x, y);
//
//    knockback_x = lengthdir_x(knockback_force, dir);
//    knockback_y = lengthdir_y(knockback_force, dir);

//    knockback_timer = 15; // Number of frames knockback lasts
//    health -= 1; // If you have a health system
//}
